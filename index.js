//code goes here

//scooter with year, color, model
function Scooter(year, color, model) {
    this.year = year;
    this.color = color;
    this.model = model;
}

//driver with name, age, experience
function Driver(name, age, experience) {
    this.name = name;
    this.age = age;
    this.experience = experience;
}

//PickupLocation with address and city
function PickupLocation(address, city) {
    this.address = address;
    this.city = city;
}